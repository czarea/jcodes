{
    # for(column in table.columns){ #
      # if( columnLP.last ) { #
      # if( column.number ) { #
      '@{utils.toUpperCaseFirst(column.columnJavaName)}':@{column.eg} //@{column.remarks},
      # } else { #
      '@{utils.toUpperCaseFirst(column.columnJavaName)}':'@{column.eg}' //@{column.remarks},
      # } #
      # } else { #
      # if( column.number ) { #
      '@{utils.toUpperCaseFirst(column.columnJavaName)}':@{column.eg}, //@{column.remarks}
      # } else { #
      '@{utils.toUpperCaseFirst(column.columnJavaName)}':'@{column.eg}', //@{column.remarks}
      # } #
      # } #
    # } #
}
